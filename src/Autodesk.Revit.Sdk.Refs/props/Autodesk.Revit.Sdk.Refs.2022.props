<Project>
    <PropertyGroup Condition="$(Configuration.EndsWith('22'))">
        <RevitVersion>2022</RevitVersion>
    </PropertyGroup>

    <PropertyGroup Condition="'$(RevitVersion)' == '2022'">
        <TargetFramework>net48</TargetFramework>
    </PropertyGroup>
</Project>