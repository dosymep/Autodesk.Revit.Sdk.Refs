<Project>
    <PropertyGroup Condition="$(Configuration.EndsWith('19'))">
        <RevitVersion>2019</RevitVersion>
    </PropertyGroup>

    <PropertyGroup Condition="'$(RevitVersion)' == '2019'">
        <TargetFramework>net47</TargetFramework>
    </PropertyGroup>
</Project>