<Project>
    <PropertyGroup Condition="$(Configuration.EndsWith('21'))">
        <RevitVersion>2021</RevitVersion>
    </PropertyGroup>

    <PropertyGroup Condition="'$(RevitVersion)' == '2021'">
        <TargetFramework>net48</TargetFramework>
    </PropertyGroup>
</Project>